const country = "Sweden";
const continent = "Europe";
const population = 10;
const isIsland = false;
const language = "Swedish";

const averagePopulation = 33 >= population ? `${country}s is below average population` : `${country}s is above average population`;

console.log(averagePopulation)