function describePopulation(country, population) {
    console.log(`${country} has ${population} million people, so its about ${percentageOfWorld1}% of the world`);
    return describePopulation;
}

function percentageOfWorld1(percent) {
    const percentage = population / 7900 * 100
    return percentageOfWorld1;
}

const china = describePopulation("china", 1440)
const sweden = describePopulation("sweden", 10)
const netherlands = describePopulation("netherlands", 17)

console.log(china, sweden, netherlands)